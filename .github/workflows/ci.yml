name: CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: macos-latest

    env:
      CI: true  # Set CI environment variable for Lua scripts

    steps:
      # 1. Checkout the repository
      - name: Checkout Repository
        uses: actions/checkout@v3

      # 2. Set up Homebrew (use the correct repository and version)
      - name: Install Homebrew
        uses: Homebrew/install@v3

      # 3. Update Homebrew and Install Neovim
      - name: Update Homebrew and Install Neovim
        run: |
          brew update
          brew install neovim
        # No need to set up any environment variables here

      # 4. Install Lua (if needed)
      - name: Install Lua
        run: brew install lua

      # 5. Install Lazy.nvim
      - name: Install Lazy.nvim
        run: |
          git clone https://github.com/folke/lazy.nvim.git ~/.local/share/nvim/site/pack/lazy/start/lazy.nvim

      # 6. Install Plugin Dependencies via Lazy.nvim
      - name: Install Plugin Dependencies
        run: |
          nvim --headless +LazyInstall +qa
        env:
          XDG_DATA_HOME: ~/.local/share

      # 7. Run Tungsten Test Suite
      - name: Run Tungsten Test Suite
        run: |
          nvim --headless -c "TungstenTestSuite all" -c "qa!"
        env:
          XDG_CONFIG_HOME: ~/.config  # Ensure Neovim can find its config

      # 8. Upload Test Logs
      - name: Upload Test Logs
        if: always()  # Ensure this step runs even if previous steps fail
        uses: actions/upload-artifact@v3
        with:
          name: test-logs
          path: logs/test_logs.txt

      # 9. Display Test Logs (Optional)
      - name: Display Test Logs
        if: always()
        run: |
          if [ -f logs/test_logs.txt ]; then
            echo "=== Test Logs ==="
            cat logs/test_logs.txt
            echo "================="
          else
            echo "No test logs found."
          fi

